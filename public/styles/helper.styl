.main {
    min-height: 50rem;
    background: $gray-lighter;
    padding-bottom: 8.5em;
}
footer {
}

.btn {
    font-family: GOVERNOR, Open Sans, sans-serif !important;
    min-width: 4em;
    border-width: 0;
}

html {
    font-size: 16px;
}

a, a:hover, a:focus {
  color: $gray-dark;
}