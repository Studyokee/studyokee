.controls {
    .progress {
        position: relative;
        overflow: visible;
        border-radius: 0;

        .progress-bar {
            -webkit-transition: none; */
            -o-transition: none;
            /* transition: none; */
        }
        .progress-timer {
            position: absolute;
            font-size: 13px;
            left: 0;
            margin-left: -66px;
            line-height: 20px;
        }
    }
}