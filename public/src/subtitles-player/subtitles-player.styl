.player {
    position: relative;

    .subtitlesContainer {
        border-bottom: $borderNormal;
        position: relative;
        overflow: hidden;
        height: 480px;

        .spinner {
            margin-top: 128px;
        }
    }
    .instructions {
        font-size: 14px;
        padding: 12px;
        color: $textSubdued;
    }
}