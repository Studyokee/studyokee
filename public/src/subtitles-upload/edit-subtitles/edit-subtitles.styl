.edit-subtitles {
    .editText {
        width: 100%;
        display: block;
        margin: 8px;
        font-size: 14px;
        line-height: 24px;
        padding: 8px;
        min-height: 1000px;
    }
}