.classroom {
    .menu {
        margin-top: 2rem;
    }
    .panel {
        border-width: 0;
        border-top-width: 0.8rem;
        border-bottom-width: 0.8rem;
    }
    .panel a * {
        color: $gray-dark;
    }
    .panel-heading {
        border-width: 0;
    }

    .center .panel {
        border-color: $yellow;
        margin-top: 2.3rem;
    }
    .center {
        margin-top: 3rem;
    }
}
