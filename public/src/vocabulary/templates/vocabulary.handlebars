<ul class="vocabulary">
    {{#each words}}
        <li data-index="{{@index}}" class="card">
            <a href="#" class="btn btn-primary btn-lg wordOrPhrase">{{this.wordOrPhrase}}</a>
            <a href="#" class="btn btn-default btn-lg definition" style="display:none;">
                <div class="definitions">
                    <h4>Definitions:</h4>
                    <span class="text-info">{{this.definition.translatedText}}</span>
                </div>
            </a>
            <button type="button" class="remove action close" title="Remove Card">×</button>
        </li>
    {{/each}}
</ul>