<ul class="vocabulary">
    {{#each words}}
        <li data-index="{{@index}}" class="card">
            <a href="#" class="btn btn-primary btn-lg wordOrPhrase">{{this.wordOrPhrase}}</a>
            <a href="#" class="btn btn-default btn-lg definition" style="display:none;">{{this.definition}}</a>
            <button type="button" class="remove action close" title="Remove Card">×</button>
        </li>
    {{/each}}
</ul>